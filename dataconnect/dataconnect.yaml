specVersion: "v1"
serviceId: "test2"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "test2-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
